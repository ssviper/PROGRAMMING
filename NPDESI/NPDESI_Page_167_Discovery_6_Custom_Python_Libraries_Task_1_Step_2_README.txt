#Create a list called devices containing three strings: r1, r2, and r3.  Print each string in a for loop, 
#sleep for one second, then continue.

#type python
#copy the following lines into the Python Interpreter

import time

devices = ['r1', 'r2', 'r3']

for item in devices:
    print (item)
    time.sleep(1)

#Note: Press enter after the above.  <-- Don't paste this line